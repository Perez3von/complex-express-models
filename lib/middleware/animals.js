import { Router } from 'express';


export default Router()

  .get('/', async (req, res, next) => {
    try {
      
      
     
      //res.json(on);
    } catch (error) {
      next(error);
    }
  });
  